# glm_meta_types

In order to integrate with cpp_tbx's reflection system (meta_utils), you have to know metadata about every "base type" (the types used to construct custom types), so here is the metadata for some basic glm types.
